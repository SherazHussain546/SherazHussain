
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // SECURITY RULES REVISION: 2024-06-03-V2
    // REASON: Resolving list operation denials on projectGoals by hardening public read permissions.
    
    function isAuthenticated() {
      return request.auth != null;
    }

    // Public collections: Accessible by anyone, manageable by admin
    match /posts/{postId} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    match /projectGoals/{goalId} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    match /siteConfig/{configId} {
      allow read: if true;
      allow write: if isAuthenticated();
    }

    // Analytics: Open creation for tracking, admin-only read
    match /analyticsEvents/{eventId} {
      allow create: if true;
      allow read: if isAuthenticated();
    }

    // Admin-only collections
    match /workflows/{workflowId} {
      allow read, write: if isAuthenticated();
    }
  }
}
